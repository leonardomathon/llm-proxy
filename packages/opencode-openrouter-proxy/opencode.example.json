{
  "$schema": "https://opencode.ai/config.json",
  "provider": {
    "openrouter": {
      "options": {
        "baseURL": "{env:OPENROUTER_PROXY_URL}",
        "apiKey": "{env:OPENROUTER_PROXY_TOKEN}"
      }
    }
  }
}
